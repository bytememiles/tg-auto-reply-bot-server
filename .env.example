# Telegram Bot API token from @BotFather
TELEGRAM_BOT_TOKEN=your_bot_token

# OpenAI (for dynamic replies); fallback to static KEYWORD_REPLIES on failure
OPENAI_API_KEY=your_openai_api_key
# OPENAI_MODEL=gpt-4o-mini

# Optional: comma-separated Telegram user IDs treated as "frequent vulgar/mean" (sterner tone)
# FREQUENT_VULGAR_USER_IDS=12345,67890

# Vercel KV / Upstash Redis (chat history + vulgar count). Omit to skip storage.
# KV_REST_API_URL=   (injected by Vercel when you add Redis/KV storage)
# KV_REST_API_TOKEN=
