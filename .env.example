# Telegram Bot API token from @BotFather
TELEGRAM_BOT_TOKEN=your_bot_token

# OpenAI (for dynamic replies); fallback to static KEYWORD_REPLIES on failure
OPENAI_API_KEY=your_openai_api_key
# OPENAI_MODEL=gpt-4o-mini

# Noisy users: comma-separated Telegram user IDs. Their messages are batched and get one evil/vulgar reply per burst.
# NOISY_USER_IDS=7176721542,8435614998

# Vercel KV / Upstash Redis (noisy-user burst cache only). Omit to skip storage.
# KV_REST_API_URL=   (injected by Vercel when you add Redis/KV storage)
# KV_REST_API_TOKEN=
